# Alkon Integration Fixes

Status: active

Fixes applied in this pass:

- Added canonical `/trading` route using the existing clean-zero Trading Workstation.
- Updated public Home workspace CTA to `/trading`.
- Updated public header Trading Workspace link to `/trading`.
- Updated Apps / Platforms Web App CTA to `/trading`.
- Updated Brain and Companion default route context to `/trading`.
- Updated Local Day One commands, route lists, and operation checklist to the official path and `/trading`.
- Updated canonical route smoke harness to include `/trading` and reuse existing dev servers more reliably.
- Updated existing route regression tests from primary `/en` expectations to `/trading`.
- Added origin-clean regression coverage and required visual proof paths.
- Added ownership/clutter/API/route laws.
- Hardened `.gitignore` for sensitive files, caches, archives, and unapproved raster media.
- Added private Jar Build System under `lib/server/jar-build`.
- Rendered Jar status and Command Passport bridge inside private Founder surfaces.
- Added Source-to-Reality doctrine and root structure reports.

No unsafe activation was added.
