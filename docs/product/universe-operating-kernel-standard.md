# Universe Operating Kernel Standard

Official path:
`C:\Users\ahmad\Desktop\ALKON\Pro Max\Pro Max Trading\pro-max-trading-platform`

Status: existing_kernel_canonicalized

## Kernel Found

The existing kernel is `lib/server/alkon-kernel/*`.

It already provides:

- Command 0-16 state
- creator runtime oath
- Zero Truth
- Reality Ownership
- Reality Trial
- Evidence Chain
- Memory Law
- Return to Heart
- One Next Action
- Command Passport
- Builder Selection
- Daily Operating Loop
- Infinite Governed Evolution
- Founder Final Authority
- Treasury Discipline
- Legal Reality Gate
- Public Trust Gate
- Local Day One Gate

## Canonical Adapter

The official Universe-facing path is `lib/server/universe/kernel/*`.

This path is an adapter only. It delegates to `lib/server/alkon-kernel/*` and maps the existing kernel into the cleaned canonical architecture:

- Ahmad Private Devices
- الكون / Universe
- Ahmad Digital Vault
- Protection Core
- Universe Operating Kernel
- Universe One
- Swiss Local Constitution
- Pro Max Galaxy
- Earth Planet
- ALKON Background Guardian

Existing kernel canonicalized as Universe Operating Kernel.

Universe Operating Kernel is the root private operating brain.

No duplicate kernel exists.

## Required Exports

The adapter exports:

- `getUniverseKernelState()`
- `getUniverseKernelRole()`
- `getUniverseKernelPermissions()`
- `getUniverseKernelGuards()`
- `getUniverseKernelNextAction()`
- `getUniverseKernelTruth()`
- `getUniverseKernelReadiness()`

## Permissions

Allowed safe internal:

- audit
- report generation
- roadmap generation
- task generation
- Product Truth check
- registry check
- validation planning
- next action generation

Requires Ahmad approval:

- code-changing execution outside an approved mission
- Git commit or push unless the mission requires it
- architecture changes
- Local Day One start
- external account connection
- official, legal, money, brand, or final decisions

Blocked:

- public launch
- billing
- receiving money
- real money
- broker execution
- legal approval claims
- FINMA, licensed, or regulated claims
- final brand adoption
- domain purchase
- public الكون / Universe
- public ALKON
- secrets in Git

## Guards

Product Truth overrides every action.

Swiss Local Constitution is above the Global Layer.

Dangerous actions require Ahmad approval or remain blocked.

Universe and ALKON remain private.

The adapter must never create command, state, gate, Local Day One, or one-next-action logic that competes with `lib/server/alkon-kernel/*`.

## Honest Gaps

The existing kernel documents blocking and authority rules, but these capabilities remain pending:

- dedicated runtime kill switch
- rollback execution
- encrypted Ahmad Digital Vault
- external account control

They must not be presented as active.

## Future Relation

Infinity Mode and Operator Mode may only resume after founder boundary hardening, visual map clarity, Ahmad decision, and remaining registry conflicts are resolved. They must use the Universe Operating Kernel adapter and must not create a parallel kernel.

## Absolute Founder Boundary

Universe Operating Kernel enforces Absolute Founder Boundary.

الكون ينفذ الأعمال الداخلية الآمنة مباشرة.

أحمد يوافق على الأمور الرسمية والقانونية والمالية والإطلاق والبروكر والاسم والقرارات النهائية.

الدفع واستلام الأموال يتطلبان موافقة أحمد دائمًا.

التداول الحقيقي وتنفيذ البروكر يتطلبان موافقة أحمد دائمًا.

الأسرار والوثائق الخاصة لا تخرج من سيطرة أحمد إلا بموافقة صريحة.

الكون لا يتجاوز Product Truth.

The kernel references `lib/server/universe/founder-boundary/*` for safe internal actions, approval-required actions, never-alone actions, and explanation helpers.
