# Pro Max Generated Soundscape

No audio file is bundled by default.

The living-universe soundscape is generated locally with the Web Audio API after user activation. It is off by default, uses low volume, stops on unmount, and does not call external audio URLs.
